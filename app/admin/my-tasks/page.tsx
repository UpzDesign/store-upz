"use client";

import Link from "next/link";
import { FormEvent, useEffect, useMemo, useState } from "react";
import { useRouter } from "next/navigation";

type Member={id:number;name:string;email?:string|null;role:string;capacity:number};
type Checklist={id:number;title:string;completed:boolean};
type Comment={id:number;body:string;author?:string|null;createdAt:string};
type Attachment={id:number;title:string;fileUrl:string;fileType?:string|null};
type Task={id:number;title:string;description?:string|null;status:string;priority:string;dueDate?:string|null;checklist:Checklist[];comments:Comment[];attachments:Attachment[];project:{id:number;title:string;status:string;dueDate?:string|null;company:{name:string;shortName:string;slug:string;primaryColor:string};engagement?:{id:number;name:string;address?:string|null}|null}};
type Data={team:Member[];member:Member|null;tasks:Task[];session:{role:"admin"|"contributor";name:string}};
const STATUSES=["todo","ready","in_progress","blocked","review","complete"];
const pretty=(value:string)=>value.replaceAll("_"," ").replace(/\b\w/g,c=>c.toUpperCase());
const overdue=(task:Task)=>Boolean(task.dueDate&&new Date(task.dueDate)<new Date()&&task.status!=="complete");

export default function MyTasksPage(){
 const router=useRouter();const[data,setData]=useState<Data|null>(null);const[member,setMember]=useState("");const[filter,setFilter]=useState("open");const[query,setQuery]=useState("");const[openTask,setOpenTask]=useState<number|null>(null);const[comment,setComment]=useState("");const[checklist,setChecklist]=useState("");
 async function load(name=member){const response=await fetch(`/api/admin/my-tasks${name?`?member=${encodeURIComponent(name)}`:""}`,{cache:"no-store"});if(response.status===401){router.replace("/admin/login");return;}const value=await response.json();setData(value);if(value?.session?.role==="admin"&&name)localStorage.setItem("upz_team_member",name);if(value?.member?.name)setMember(value.member.name)}
 useEffect(()=>{const saved=localStorage.getItem("upz_team_member")||"";load(saved)},[]);
 async function logout(){await fetch("/api/staff/logout",{method:"POST"});router.replace("/admin/login");router.refresh()}
 async function patchTask(task:Task,body:Record<string,unknown>){await fetch(`/api/admin/tasks/${task.id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});await load()}
 async function action(task:Task,body:Record<string,unknown>){await fetch(`/api/admin/tasks/${task.id}/workspace`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});await load()}
 async function addComment(event:FormEvent,task:Task){event.preventDefault();if(!comment.trim()||!data?.member)return;await action(task,{action:"add_comment",message:comment.trim(),author:data.member.name});setComment("")}
 async function addChecklist(event:FormEvent,task:Task){event.preventDefault();if(!checklist.trim())return;await action(task,{action:"add_checklist",title:checklist.trim()});setChecklist("")}
 const tasks=useMemo(()=>!data?[]:data.tasks.filter(task=>{const text=`${task.title} ${task.project.title} ${task.project.company.name} ${task.project.engagement?.name||""}`.toLowerCase();const status=filter==="all"||filter==="open"&&task.status!=="complete"||filter==="complete"&&task.status==="complete"||filter==="overdue"&&overdue(task);return text.includes(query.toLowerCase())&&status}),[data,filter,query]);
 const completed=data?.tasks.filter(task=>task.status==="complete").length||0;const late=data?.tasks.filter(overdue).length||0;
 if(!data)return <main className="my-tasks-page"><section className="admin-simple-state"><h1>Loading assignments...</h1></section></main>;
 return <main className="my-tasks-page"><section className="my-tasks-shell"><header className="my-tasks-hero"><div><span>UPZ PRODUCTION</span><h1>My Assigned Stages</h1><p>Signed in as {data.session.name}. Manage stages assigned across portfolio work orders, including status, checklists, notes, files, and deadlines.</p></div><div>{data.session.role==="admin"&&<><Link href="/admin/operations">Operations</Link><Link href="/admin/team">Team</Link></>}<button onClick={logout}>Sign Out</button></div></header>
 {data.session.role==="admin"&&<section className="my-tasks-member"><label>Admin preview<select value={member} onChange={e=>{setMember(e.target.value);load(e.target.value)}}><option value="">Select team member</option>{data.team.map(item=><option key={item.id} value={item.name}>{item.name} — {item.role}</option>)}</select></label>{data.member&&<div><strong>{data.member.name}</strong><span>{data.member.role}</span><small>Capacity: {data.member.capacity} active stages</small></div>}</section>}
 {!data.member?<section className="my-tasks-empty"><h2>No contributor selected</h2><p>Choose a team member to preview their secured assignment workspace.</p></section>:<><section className="my-tasks-kpis"><article><span>Assigned</span><strong>{data.tasks.length}</strong></article><article><span>Open</span><strong>{data.tasks.length-completed}</strong></article><article><span>Complete</span><strong>{completed}</strong></article><article><span>Overdue</span><strong>{late}</strong></article></section><div className="my-tasks-toolbar"><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search portfolios, work orders, or stages..."/><nav>{["open","overdue","complete","all"].map(value=><button key={value} className={filter===value?"active":""} onClick={()=>setFilter(value)}>{pretty(value)}</button>)}</nav></div><section className="my-task-list">{tasks.map(task=>{const done=task.checklist.filter(item=>item.completed).length;const expanded=openTask===task.id;return <article key={task.id} className={`${expanded?"expanded":""} ${overdue(task)?"overdue":""}`} style={{"--task-brand":task.project.company.primaryColor} as React.CSSProperties}><button className="my-task-summary" onClick={()=>setOpenTask(expanded?null:task.id)}><span className={`status ${task.status}`}>{pretty(task.status)}</span><div><small>{task.project.company.shortName} · {task.project.engagement?.name||"General Portfolio"}</small><h2>{task.title}</h2><p><strong>{task.project.title}</strong>{task.project.engagement?.address?` · ${task.project.engagement.address}`:""}</p></div><div className="my-task-meta"><strong>{task.dueDate?new Date(task.dueDate).toLocaleDateString():"No due date"}</strong><span>{done}/{task.checklist.length} checklist</span></div></button>{expanded&&<div className="my-task-detail"><section><h3>Stage status</h3><select value={task.status} onChange={e=>patchTask(task,{status:e.target.value})}>{STATUSES.map(value=><option key={value} value={value}>{pretty(value)}</option>)}</select><p>{task.description||"No additional stage instructions."}</p>{data.session.role==="admin"&&<Link href={`/admin/project/${task.project.id}?tab=tasks`}>Open full work order →</Link>}</section><section><h3>Checklist</h3>{task.checklist.map(item=><button key={item.id} className={item.completed?"checked":""} onClick={()=>action(task,{action:"toggle_checklist",itemId:item.id})}>{item.completed?"✓":"○"} {item.title}</button>)}<form onSubmit={e=>addChecklist(e,task)}><input value={checklist} onChange={e=>setChecklist(e.target.value)} placeholder="Add checklist item"/><button>Add</button></form></section><section><h3>Production notes</h3>{task.comments.map(item=><article key={item.id}><p>{item.body}</p><small>{item.author||"Team"} · {new Date(item.createdAt).toLocaleString()}</small></article>)}<form onSubmit={e=>addComment(e,task)}><textarea value={comment} onChange={e=>setComment(e.target.value)} placeholder="Add a stage note..."/><button>Post Note</button></form></section><section><h3>Attachments</h3>{task.attachments.length?task.attachments.map(item=><a key={item.id} href={item.fileUrl} target="_blank" rel="noreferrer"><span>{item.title}</span><small>{item.fileType||"File"} ↗</small></a>):<p>No stage attachments.</p>}</section></div>}</article>})}{!tasks.length&&<div className="my-tasks-empty"><h2>No matching stages</h2><p>There are no assignments in this view.</p></div>}</section></>}
 </section></main>;
}
