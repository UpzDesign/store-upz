"use client";
import{useEffect,useState}from"react";import Link from"next/link";import{useParams,useSearchParams}from"next/navigation";
type MarketingRequest={id:number;type:string;title:string;description?:string|null;priority:string;status:string;createdAt:string;updatedAt:string;project?:{id:number;status:string}|null};type Company={name:string;slug:string;shortName:string;primaryColor:string;requests?:MarketingRequest[]};const priorityWeight:Record<string,number>={urgent:0,high:1,normal:2};const pending=(r:MarketingRequest)=>!["approved","declined","cancelled","complete","completed","closed"].includes(r.status.toLowerCase())&&!r.project;function preview(value?:string|null){return(value||"").split("__UPZ_CONTEXT__")[0].trim()||"Open the request to review all submitted details.";}
export default function AdminRequestsPage(){const params=useParams(),searchParams=useSearchParams();const slug=Array.isArray(params?.slug)?params.slug[0]:params?.slug;const focusedId=Number(searchParams.get("request")||0);const[company,setCompany]=useState<Company|null>(null);const[message,setMessage]=useState("");const[loading,setLoading]=useState(true);function loadCompany(){if(!slug)return;setLoading(true);fetch(`/api/admin/companies/${slug}`,{cache:"no-store"}).then(r=>{if(!r.ok)throw new Error("Company not found");return r.json()}).then(setCompany).catch(e=>setMessage(e?.message||"Unable to load requests")).finally(()=>setLoading(false))}useEffect(()=>{loadCompany()},[slug]);if(loading)return <main className="admin-page"><section className="admin-simple-state"><h1>Loading requests...</h1></section></main>;if(!company)return <main className="admin-page"><section className="admin-simple-state"><h1>Company not found</h1></section></main>;const requests=[...(company.requests||[])].sort((a,b)=>Number(pending(b))-Number(pending(a))||((priorityWeight[a.priority]??2)-(priorityWeight[b.priority]??2))||new Date(b.createdAt).getTime()-new Date(a.createdAt).getTime());const newCount=requests.filter(pending).length;return <main className="admin-page"><section className="admin-company-detail"><header className="admin-detail-hero"><div className="admin-detail-logo" style={{borderColor:company.primaryColor}}><span>{company.shortName}</span></div><div><div className="admin-eyebrow">Project Requests</div><h1>{company.shortName} Requests</h1><p>Review pending submissions, then approve them into projects or decline them with a client-visible reason.</p></div></header>{newCount>0&&<section className="admin-request-alert"><div><span>Action Required</span><h2>{newCount} new request{newCount===1?"":"s"} awaiting review</h2><p>Open each request to make the final decision and set project ownership.</p></div><Link href="/admin/inbox">View Admin Inbox</Link></section>}<section className="admin-section"><div className="admin-section-heading"><div><span>Request History</span><h2>{requests.length} total</h2></div></div>{message&&<p className="admin-error">{message}</p>}<div className="admin-request-list">{!requests.length?<p>No project requests have been submitted yet.</p>:requests.map(request=><article id={`request-${request.id}`} key={request.id} className={`admin-request-card ${pending(request)?"is-new":""} priority-${request.priority} ${focusedId===request.id?"is-focused":""}`}><div className="admin-request-card-head"><div><div className="admin-request-flags"><b>{pending(request)?"Pending Review":request.status.replaceAll("_"," ")}</b><em>{request.priority} priority</em><span>{request.type}</span></div><h3>{request.title}</h3></div><small>{new Date(request.createdAt).toLocaleString()}</small></div><p className="admin-request-preview">{preview(request.description)}</p><div className="admin-request-controls">{request.project?<Link className="admin-primary-button" href={`/admin/operations?project=${request.project.id}`}>Open Project</Link>:<Link className="admin-primary-button" href={`/admin/request/${request.id}`}>{pending(request)?"Review Request":"View Decision"}</Link>}</div></article>)}</div></section></section></main>;}