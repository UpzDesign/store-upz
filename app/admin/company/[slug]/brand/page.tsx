"use client";
import{useEffect,useState}from"react";import{useParams}from"next/navigation";import Link from"next/link";import{AdminButton,AdminField,AdminFormGrid,AdminHeader,AdminPage,AdminSection,AdminSectionHeader}from"@/components/admin/AdminUI";
type Company={name:string;slug:string;shortName:string;logo?:string|null;logoType:string;logoText?:string|null;logoTextColor?:string|null;logoFontStyle?:string|null;primaryColor:string;secondaryColor:string;heroTitle:string;heroText:string;portalEnabled:boolean};
export default function CompanyBrandPage(){const params=useParams(),slug=Array.isArray(params.slug)?params.slug[0]:String(params.slug||"");const[company,setCompany]=useState<Company|null>(null),[saving,setSaving]=useState(false),[message,setMessage]=useState("");useEffect(()=>{fetch(`/api/admin/companies/${slug}`,{cache:"no-store"}).then(r=>r.json()).then(setCompany)},[slug]);if(!company)return <AdminPage><p>Loading brand settings...</p></AdminPage>;const set=(key:keyof Company,value:any)=>setCompany(c=>c?{...c,[key]:value}:c);async function save(e:React.FormEvent){e.preventDefault();setSaving(true);setMessage("");const r=await fetch(`/api/admin/companies/${slug}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(company)}),d=await r.json();setSaving(false);setMessage(r.ok?"Brand and contrast settings saved.":d?.error||"Unable to save");if(r.ok)setCompany(d)}return <AdminPage><AdminHeader eyebrow="Company Settings" title={`${company.name} Brand`} description="Control the logo, wordmark color, and primary contrast used throughout the client portal." actions={<AdminButton href={`/admin/company/${slug}`}>Back to Company</AdminButton>}/><form onSubmit={save}><AdminSection><AdminSectionHeader eyebrow="Identity" title="Logo and wordmark"/><AdminFormGrid><AdminField label="Logo Type"><select value={company.logoType} onChange={e=>set("logoType",e.target.value)}><option value="image">Image logo</option><option value="text">Text wordmark</option><option value="none">No logo</option></select></AdminField><AdminField label="Logo Path"><input value={company.logo||""} onChange={e=>set("logo",e.target.value)}/></AdminField><AdminField label="Wordmark Text"><input value={company.logoText||company.shortName} onChange={e=>set("logoText",e.target.value)}/></AdminField><AdminField label="Wordmark Style"><select value={company.logoFontStyle||"sans"} onChange={e=>set("logoFontStyle",e.target.value)}><option value="sans">Sans Bold</option><option value="serif">Serif</option><option value="light">Sans Light</option><option value="condensed">Condensed Bold</option></select></AdminField><AdminField label="Primary Brand Color"><input type="color" value={company.primaryColor} onChange={e=>set("primaryColor",e.target.value)}/></AdminField><AdminField label="Secondary Color"><input type="color" value={company.secondaryColor} onChange={e=>set("secondaryColor",e.target.value)}/></AdminField><AdminField label="Wordmark / On-Brand Color" className="wide"><div style={{display:"flex",gap:12,alignItems:"center"}}><input type="color" value={company.logoTextColor||"#ffffff"} onChange={e=>set("logoTextColor",e.target.value)}/><button type="button" className="admin-ui-btn admin-ui-btn-outline" onClick={()=>set("logoTextColor","#111111")}>Dark text</button><button type="button" className="admin-ui-btn admin-ui-btn-outline" onClick={()=>set("logoTextColor","#ffffff")}>Light text</button></div><small>This color is used over the primary brand color to prevent dark-on-dark or light-on-light combinations.</small></AdminField></AdminFormGrid><div style={{marginTop:20,padding:28,borderRadius:12,background:company.primaryColor,color:company.logoTextColor||"#ffffff"}}><strong style={{fontSize:24}}>{company.logoText||company.shortName}</strong><p>Primary brand contrast preview</p></div>{message&&<p>{message}</p>}<div style={{marginTop:20}}><AdminButton type="submit" disabled={saving}>{saving?"Saving...":"Save Brand Settings"}</AdminButton></div></AdminSection></form></AdminPage>}
