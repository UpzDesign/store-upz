"use client";
import{useEffect,useState}from"react";import Link from"next/link";import{useParams,useRouter}from"next/navigation";
type RequestDetail={id:number;type:string;title:string;description?:string|null;priority:string;status:string;createdAt:string;company:{name:string;shortName:string;slug:string;logo?:string|null;primaryColor:string};project?:{id:number}|null};type Member={id:number;name:string;role:string};
function visibleDescription(value?:string|null){return(value||"").split("__UPZ_CONTEXT__")[0].trim()||"No additional description provided."}function decisionReason(value?:string|null){const marker="__UPZ_DECISION__";const source=value||"";const index=source.lastIndexOf(marker);if(index<0)return"";try{return JSON.parse(source.slice(index+marker.length).trim()).reason||""}catch{return""}}
export default function RequestReviewPage(){const params=useParams(),router=useRouter();const id=Array.isArray(params?.id)?params.id[0]:params?.id;const[item,setItem]=useState<RequestDetail|null>(null),[team,setTeam]=useState<Member[]>([]),[loading,setLoading]=useState(true),[message,setMessage]=useState(""),[working,setWorking]=useState<"approve"|"decline"|null>(null),[assignedTo,setAssignedTo]=useState(""),[startDate,setStartDate]=useState(""),[dueDate,setDueDate]=useState(""),[declineReason,setDeclineReason]=useState("");useEffect(()=>{if(!id)return;Promise.all([fetch(`/api/admin/requests/${id}`,{cache:"no-store"}).then(r=>{if(!r.ok)throw new Error("Unable to load request");return r.json()}),fetch("/api/admin/operations",{cache:"no-store"}).then(r=>r.ok?r.json():{team:[]})]).then(([requestData,ops])=>{setItem(requestData);setTeam(Array.isArray(ops?.team)?ops.team:[]);setDeclineReason(decisionReason(requestData.description))}).catch(e=>setMessage(e?.message||"Unable to load request")).finally(()=>setLoading(false))},[id]);async function approve(){if(!item||working)return;setWorking("approve");setMessage("");try{const response=await fetch(`/api/admin/requests/${item.id}/convert`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({assignedTo,startDate,dueDate,priority:item.priority,clientVisible:true})});const data=await response.json();if(!response.ok)throw new Error(data?.error||"Unable to approve request");router.push(`/admin/operations?project=${data.id}`)}catch(e:any){setMessage(e?.message||"Unable to approve request");setWorking(null)}}async function decline(){if(!item||working||!declineReason.trim())return;setWorking("decline");setMessage("");try{const clean=visibleDescription(item.description);const description=`${clean}\n\n__UPZ_DECISION__${JSON.stringify({reason:declineReason.trim()})}`;const response=await fetch(`/api/admin/requests/${item.id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status:"declined",description})});const data=await response.json();if(!response.ok)throw new Error(data?.error||"Unable to decline request");setItem({...item,...data});setMessage("Request declined. The client will see the reason in their project updates.");setWorking(null)}catch(e:any){setMessage(e?.message||"Unable to decline request");setWorking(null)}}if(loading)return <main className="admin-page"><section className="admin-simple-state"><h1>Loading request...</h1></section></main>;if(!item)return <main className="admin-page"><section className="admin-simple-state"><h1>Request not found</h1>{message&&<p className="admin-error">{message}</p>}</section></main>;const decided=item.project||["declined","cancelled"].includes(item.status.toLowerCase());return <main className="admin-page"><section className="admin-company-detail request-review-page"><header className="admin-detail-hero"><div className="admin-detail-logo" style={{borderColor:item.company.primaryColor}}><img src={item.company.logo||"/upz-logo.svg"} alt=""/></div><div><div className="admin-eyebrow">Request Review</div><h1>{item.title}</h1><p>{item.company.name} · {item.type} · submitted {new Date(item.createdAt).toLocaleString()}</p></div></header><section className="admin-detail-grid request-approval-grid"><article className="admin-detail-card"><span>Client Intake</span><h2>Request details</h2><div className="request-detail-badges"><b>{item.status}</b><em>{item.priority} priority</em></div><pre className="request-detail-pre">{visibleDescription(item.description)}</pre></article><article className="admin-detail-card request-decision-card"><span>Final Decision</span><h2>{item.project?"Approved Project":item.status==="declined"?"Request Declined":"Approve or decline"}</h2>{item.project?<><p>This request is active and managed in Operations.</p><Link className="admin-primary-button" href={`/admin/operations?project=${item.project.id}`}>Open Project</Link></>:item.status==="declined"?<><p><strong>Client-visible reason:</strong></p><p>{declineReason||"No reason provided."}</p></>:<><p>Confirm the project owner and schedule before creating the work order.</p><div className="request-approval-fields"><label>Project manager<select value={assignedTo} onChange={e=>setAssignedTo(e.target.value)}><option value="">Unassigned</option>{team.map(member=><option value={member.name} key={member.id}>{member.name} — {member.role}</option>)}</select></label><label>Start date<input type="date" value={startDate} onChange={e=>setStartDate(e.target.value)}/></label><label>Due date<input type="date" value={dueDate} onChange={e=>setDueDate(e.target.value)}/></label></div><button className="admin-primary-button" type="button" onClick={approve} disabled={Boolean(working)}>{working==="approve"?"Creating Project...":"Approve & Create Project"}</button><div className="request-decline-box"><label>Reason for declining<textarea value={declineReason} onChange={e=>setDeclineReason(e.target.value)} placeholder="Explain why this request cannot move forward. This message will be visible to the client."/></label><button className="admin-danger-button" type="button" onClick={decline} disabled={Boolean(working)||!declineReason.trim()}>{working==="decline"?"Declining...":"Decline Request"}</button></div>{message&&<p className={message.startsWith("Request declined")?"admin-inline-success":"admin-error"}>{message}</p>}</>}</article></section></section></main>;}