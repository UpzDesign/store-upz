"use client";

import Link from "next/link";
import { DragEvent, FormEvent, useEffect, useMemo, useState } from "react";

const COLUMNS=[["new","New"],["in_progress","In Progress"],["waiting_client","Waiting Client"],["review","Review"],["complete","Complete"]] as const;
const PRIORITIES=["urgent","high","normal","low"];
type Task={id:number;title:string;status:string;priority:string;assignedTo?:string|null;dueDate?:string|null};
type Project={id:number;title:string;description?:string|null;status:string;priority:string;assignedTo?:string|null;startDate?:string|null;dueDate?:string|null;budget?:number|null;internalCost?:number|null;clientVisible:boolean;company:{name:string;shortName:string;slug:string};engagement?:{id:number;name:string}|null;tasks:Task[];notes:Array<{body:string;createdAt:string}>};
type Member={id:number;name:string;email?:string|null;role:string;capacity:number;active:number;overdue:number;tasks:number};
type Data={projects:Project[];engagements:any[];assets:any[];services:any[];team:Member[]};
const money=(value:number)=>new Intl.NumberFormat("en-US",{style:"currency",currency:"USD",maximumFractionDigits:0}).format(value||0);
const dateInput=(value?:string|null)=>value?new Date(value).toISOString().slice(0,10):"";
const isComplete=(status:string)=>["complete","completed","cancelled"].includes(status);
const isOverdue=(project:Project)=>Boolean(project.dueDate&&new Date(project.dueDate)<new Date()&&!isComplete(project.status));

export default function OperationsPage(){
 const [data,setData]=useState<Data|null>(null);const [query,setQuery]=useState("");const [assignee,setAssignee]=useState("all");const [priority,setPriority]=useState("all");const [attentionOnly,setAttentionOnly]=useState(false);const [tab,setTab]=useState("board");const [selected,setSelected]=useState<Project|null>(null);const [draft,setDraft]=useState<any>({});const [draggedId,setDraggedId]=useState<number|null>(null);const [saving,setSaving]=useState(false);const [newTask,setNewTask]=useState("");
 const load=()=>fetch("/api/admin/operations",{cache:"no-store"}).then(r=>r.json()).then((value)=>{setData(value);if(selected){const updated=value.projects.find((p:Project)=>p.id===selected.id);if(updated){setSelected(updated);setDraft(updated)}}});
 useEffect(()=>{load()},[]);
 const projects=useMemo(()=>!data?[]:data.projects.filter(project=>{const text=`${project.title} ${project.company.name} ${project.engagement?.name||""}`.toLowerCase();return text.includes(query.toLowerCase())&&(assignee==="all"||project.assignedTo===assignee)&&(priority==="all"||project.priority===priority)&&(!attentionOnly||isOverdue(project)||["urgent","high"].includes(project.priority))}),[data,query,assignee,priority,attentionOnly]);
 function open(project:Project){setSelected(project);setDraft({...project,startDate:dateInput(project.startDate),dueDate:dateInput(project.dueDate)})}
 async function patchProject(id:number,body:any){setSaving(true);await fetch(`/api/admin/work-orders/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});setSaving(false);await load()}
 async function move(id:number,status:string){await patchProject(id,{status})}
 function startDrag(event:DragEvent<HTMLButtonElement>,id:number){setDraggedId(id);event.dataTransfer.setData("text/plain",String(id))}
 function drop(event:DragEvent<HTMLElement>,status:string){event.preventDefault();const id=Number(event.dataTransfer.getData("text/plain")||draggedId);if(id)move(id,status)}
 async function saveProject(e:FormEvent){e.preventDefault();if(!selected)return;await patchProject(selected.id,{title:draft.title,description:draft.description,status:draft.status,priority:draft.priority,assignedTo:draft.assignedTo,startDate:draft.startDate,dueDate:draft.dueDate,budget:draft.budget,internalCost:draft.internalCost,clientVisible:draft.clientVisible})}
 async function updateTask(task:Task,body:any){await fetch(`/api/admin/tasks/${task.id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});await load()}
 async function addTask(e:FormEvent){e.preventDefault();if(!selected||!newTask.trim())return;await fetch(`/api/admin/work-orders/${selected.id}/tasks`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({title:newTask})});setNewTask("");await load()}
 if(!data)return <main className="ops-page"><p>Loading operations...</p></main>;
 const active=projects.filter(p=>!isComplete(p.status)).length,overdue=projects.filter(isOverdue).length,revenue=projects.reduce((s,p)=>s+Number(p.budget||0),0),cost=projects.reduce((s,p)=>s+Number(p.internalCost||0),0);
 return <main className="ops-page"><header className="ops-head"><div><span>UPZ WORKSPACE</span><h1>Operations</h1><p>Manage work orders, assignments, tasks, dates, progress, approvals, and production status.</p></div><Link href="/admin/team">Manage team →</Link></header><section className="ops-kpis"><article><span>Active work orders</span><strong>{active}</strong></article><article><span>Needs attention</span><strong>{overdue}</strong></article><article><span>Tracked revenue</span><strong>{money(revenue)}</strong></article><article><span>Projected margin</span><strong>{money(revenue-cost)}</strong></article><article><span>Team members</span><strong>{data.team.length}</strong></article></section><nav className="ops-tabs">{[["board","Production Board"],["team","Team"],["assets","Assets"],["approvals","Approvals"]].map(([id,label])=><button className={tab===id?"active":""} onClick={()=>setTab(id)} key={id}>{label}</button>)}</nav><div className="ops-toolbar"><input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search work orders, companies, engagements..."/><select value={assignee} onChange={e=>setAssignee(e.target.value)}><option value="all">All team members</option>{data.team.map(m=><option value={m.name} key={m.id}>{m.name}</option>)}</select><select value={priority} onChange={e=>setPriority(e.target.value)}><option value="all">All priorities</option>{PRIORITIES.map(p=><option value={p} key={p}>{p}</option>)}</select><button className={attentionOnly?"active":""} onClick={()=>setAttentionOnly(!attentionOnly)}>Needs attention</button></div>{tab==="board"&&<section className="ops-board">{COLUMNS.map(([status,label])=><article className="ops-column" key={status} onDragOver={e=>e.preventDefault()} onDrop={e=>drop(e,status)}><header><strong>{label}</strong><span>{projects.filter(p=>p.status===status).length}</span></header><div className="ops-column-body">{projects.filter(p=>p.status===status).map(project=>{const done=project.tasks.filter(t=>["complete","completed"].includes(t.status)).length;return <button className={`ops-card priority-${project.priority} ${isOverdue(project)?"is-overdue":""}`} key={project.id} onClick={()=>open(project)} draggable onDragStart={e=>startDrag(e,project.id)}><small>{project.company.shortName} · {project.priority}</small><h3>{project.title}</h3><p>{project.engagement?.name||"General engagement"}</p><div className="ops-card-meta"><span>{project.assignedTo||"Unassigned"}</span><span>{project.dueDate?new Date(project.dueDate).toLocaleDateString():"No due date"}</span></div><div className="ops-task-progress"><span><i style={{width:`${project.tasks.length?Math.round(done/project.tasks.length*100):0}%`}}/></span><strong>{done}/{project.tasks.length}</strong></div></button>})}</div></article>)}</section>}{tab==="team"&&<section className="ops-grid">{data.team.map(member=><article className="ops-panel" key={member.id}><div className="ops-avatar">{member.name.split(" ").map(p=>p[0]).join("").slice(0,2)}</div><h3>{member.name}</h3><p>{member.role}</p><dl><div><dt>Active work orders</dt><dd>{member.active}</dd></div><div><dt>Open tasks</dt><dd>{member.tasks}</dd></div><div><dt>Overdue</dt><dd>{member.overdue}</dd></div><div><dt>Capacity</dt><dd>{member.capacity}</dd></div></dl></article>)}</section>}{tab==="assets"&&<section className="ops-assets">{data.assets.map(asset=><article key={asset.id}><small>{asset.engagement.company.shortName} · {asset.category}</small><h3>{asset.title}</h3><p>{asset.engagement.name}</p>{asset.fileUrl&&<a href={asset.fileUrl} target="_blank">Open asset →</a>}</article>)}</section>}{tab==="approvals"&&<section className="ops-approval-list">{projects.filter(p=>["waiting_client","review"].includes(p.status)).map(project=><article key={project.id}><div><small>{project.company.shortName}</small><h3>{project.title}</h3></div><button onClick={()=>open(project)}>Manage</button></article>)}</section>}{selected&&<div className="ops-drawer-backdrop" onMouseDown={()=>setSelected(null)}><aside className="ops-drawer ops-manager" onMouseDown={e=>e.stopPropagation()}><button className="ops-close" onClick={()=>setSelected(null)}>×</button><small>{selected.company.name}</small><h2>Manage work order</h2><form className="ops-manager-form" onSubmit={saveProject}><label>Title<input value={draft.title||""} onChange={e=>setDraft({...draft,title:e.target.value})}/></label><label>Description<textarea value={draft.description||""} onChange={e=>setDraft({...draft,description:e.target.value})}/></label><div className="ops-manager-grid"><label>Status<select value={draft.status||"new"} onChange={e=>setDraft({...draft,status:e.target.value})}>{COLUMNS.map(([v,l])=><option value={v} key={v}>{l}</option>)}</select></label><label>Priority<select value={draft.priority||"normal"} onChange={e=>setDraft({...draft,priority:e.target.value})}>{PRIORITIES.map(v=><option value={v} key={v}>{v}</option>)}</select></label><label>Assigned to<select value={draft.assignedTo||""} onChange={e=>setDraft({...draft,assignedTo:e.target.value})}><option value="">Unassigned</option>{data.team.map(m=><option value={m.name} key={m.id}>{m.name}</option>)}</select></label><label>Start date<input type="date" value={draft.startDate||""} onChange={e=>setDraft({...draft,startDate:e.target.value})}/></label><label>Due date<input type="date" value={draft.dueDate||""} onChange={e=>setDraft({...draft,dueDate:e.target.value})}/></label><label>Budget<input type="number" value={draft.budget??""} onChange={e=>setDraft({...draft,budget:e.target.value})}/></label><label>Internal cost<input type="number" value={draft.internalCost??""} onChange={e=>setDraft({...draft,internalCost:e.target.value})}/></label></div><label className="ops-check"><input type="checkbox" checked={draft.clientVisible!==false} onChange={e=>setDraft({...draft,clientVisible:e.target.checked})}/> Visible to client</label><button disabled={saving}>{saving?"Saving...":"Save work order"}</button></form><section className="ops-task-manager"><h3>Tasks</h3>{selected.tasks.map(task=><article key={task.id}><button className={task.status==="complete"?"complete":""} onClick={()=>updateTask(task,{status:task.status==="complete"?"todo":"complete"})}>{task.status==="complete"?"✓":"○"}</button><div><strong>{task.title}</strong><div><select value={task.assignedTo||""} onChange={e=>updateTask(task,{assignedTo:e.target.value})}><option value="">Unassigned</option>{data.team.map(m=><option value={m.name} key={m.id}>{m.name}</option>)}</select><input type="date" value={dateInput(task.dueDate)} onChange={e=>updateTask(task,{dueDate:e.target.value})}/></div></div></article>)}<form onSubmit={addTask}><input value={newTask} onChange={e=>setNewTask(e.target.value)} placeholder="Add a task..."/><button>Add</button></form></section>{selected.engagement&&<Link href={`/admin/engagements/${selected.engagement.id}`}>Open full engagement →</Link>}</aside></div>}</main>
}
