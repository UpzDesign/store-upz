import { NextRequest, NextResponse } from "next/server";
import { prisma } from "@/lib/prisma";
import { getWorkflowTemplate, type WorkflowStep } from "@/lib/workflow-templates";

function slugify(value:string){return value.toLowerCase().trim().replace(/[^a-z0-9]+/g,"-").replace(/^-+|-+$/g,"").slice(0,80)||`portfolio-${Date.now()}`;}
function parseRequestDescription(value?:string|null){const source=value||"";const marker="__UPZ_CONTEXT__";const index=source.lastIndexOf(marker);if(index<0)return{description:source||null,context:null as any};const description=source.slice(0,index).trim()||null;try{return{description,context:JSON.parse(source.slice(index+marker.length).trim())};}catch{return{description:source||null,context:null as any};}}
function approvalStages(value:unknown,fallback:WorkflowStep[]):WorkflowStep[]{if(!Array.isArray(value))return fallback;const stages=value.map((stage:any,index)=>({title:String(stage?.title||"").trim(),description:String(stage?.description||"").trim(),durationDays:Number.isFinite(Number(stage?.durationDays))?Math.max(0,Number(stage.durationDays)):index})).filter(stage=>stage.title);return stages.length?stages:fallback;}

export async function POST(request:NextRequest,context:{params:Promise<{id:string}>}){try{const{id}=await context.params;const requestId=Number(id);const body=await request.json().catch(()=>({}));if(!Number.isInteger(requestId)||requestId<=0)return NextResponse.json({error:"Invalid request id"},{status:400});const source=await prisma.marketingRequest.findUnique({where:{id:requestId}});if(!source)return NextResponse.json({error:"Request not found"},{status:404});const existingProject=await prisma.project.findUnique({where:{requestId:source.id}});if(existingProject)return NextResponse.json(existingProject);const parsed=parseRequestDescription(source.description);const stored=parsed.context||{};const workflow=approvalStages(body?.stages,getWorkflowTemplate(source.type||source.title));const startDate=body?.startDate?new Date(`${body.startDate}T12:00:00`):new Date();const dueDate=body?.dueDate?new Date(`${body.dueDate}T12:00:00`):new Date(startDate.getTime()+Math.max(...workflow.map(step=>step.durationDays||0),7)*86400000);const requestedPortfolioId=body?.engagementId?Number(body.engagementId):stored.engagementId?Number(stored.engagementId):null;const portfolioName=String(body?.engagementName||body?.portfolioName||stored.portfolioName||stored.engagementName||"").trim();const propertyAddress=String(body?.propertyAddress||stored.propertyAddress||"").trim();const locationName=String(body?.locationName||stored.locationName||"").trim();const assignedTo=body?.assignedTo?String(body.assignedTo).trim():null;const title=String(body?.title||source.title||`${locationName||"Work Order"} · ${source.type}`).trim();const project=await prisma.$transaction(async tx=>{let portfolio=requestedPortfolioId?await tx.engagement.findFirst({where:{id:requestedPortfolioId,companyId:source.companyId}}):null;if(!portfolio){if(!portfolioName)throw new Error("Portfolio name is required");const baseSlug=slugify(portfolioName);portfolio=await tx.engagement.findFirst({where:{companyId:source.companyId,OR:[{slug:baseSlug},{name:{equals:portfolioName,mode:"insensitive"}}]}});if(!portfolio){let uniqueSlug=baseSlug;let suffix=2;while(await tx.engagement.findUnique({where:{companyId_slug:{companyId:source.companyId,slug:uniqueSlug}}}))uniqueSlug=`${baseSlug}-${suffix++}`;portfolio=await tx.engagement.create({data:{companyId:source.companyId,name:portfolioName,slug:uniqueSlug,type:String(body?.engagementType||stored.engagementType||(propertyAddress?"property":"portfolio")),address:propertyAddress||null,description:locationName?`Portfolio containing work orders for locations and assets including ${locationName}.`:null}});}else if(propertyAddress&&!portfolio.address){portfolio=await tx.engagement.update({where:{id:portfolio.id},data:{address:propertyAddress}});}}const created=await tx.project.create({data:{companyId:source.companyId,engagementId:portfolio.id,requestId:source.id,packageId:stored.packageId?Number(stored.packageId):null,title,description:parsed.description,status:"new",priority:String(body?.priority||source.priority||"normal"),assignedTo,startDate,dueDate,clientVisible:body?.clientVisible===undefined?true:Boolean(body.clientVisible),tasks:{create:workflow.map((step,index)=>{const taskDueDate=new Date(startDate);taskDueDate.setDate(taskDueDate.getDate()+(step.durationDays||index));return{title:step.title,description:step.description,status:index===0?"in_progress":"todo",priority:source.priority||"normal",assignedTo,dueDate:taskDueDate,sortOrder:index};})},activities:{create:[{type:"project_created",message:`Work order created inside portfolio ${portfolio.name} with ${workflow.length} approved stages.`,actor:"UPZ Admin"},{type:"assignment_changed",message:assignedTo?`Assigned to ${assignedTo}.`:"Work order created without an assignee.",actor:"UPZ Admin"},{type:"portfolio_linked",message:`Portfolio: ${portfolio.name}${locationName?` · Location: ${locationName}`:""}.`,actor:"UPZ Admin"},{type:"schedule_created",message:`Scheduled from ${startDate.toLocaleDateString("en-US")} to ${dueDate.toLocaleDateString("en-US")}.`,actor:"UPZ Admin"}]}}});await tx.marketingRequest.update({where:{id:source.id},data:{status:"approved"}});return created;});return NextResponse.json(project,{status:201});}catch(error:any){console.error("Convert request error:",error);if(error?.code==="P2002"){const{id}=await context.params;const existing=await prisma.project.findUnique({where:{requestId:Number(id)}});if(existing)return NextResponse.json(existing);}return NextResponse.json({error:error?.message||"Unable to create work order"},{status:500});}}
