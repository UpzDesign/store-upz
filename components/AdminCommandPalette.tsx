"use client";
import{useEffect,useMemo,useState}from"react";import Link from"next/link";import{usePathname}from"next/navigation";
type Command={type?:string;label:string;detail:string;href:string;keywords?:string};
const base:Command[]=[{label:"Admin Dashboard",detail:"Overview and companies",href:"/admin",keywords:"home dashboard"},{label:"Operations",detail:"Kanban, team, assets and approvals",href:"/admin/operations",keywords:"production board workload assets approvals"},{label:"Engagements",detail:"Property and campaign workspaces",href:"/admin/engagements",keywords:"projects properties campaigns"},{label:"Service Library",detail:"Manage shared intake services",href:"/admin/services",keywords:"photography signage website branding"},{label:"New Company",detail:"Create a client workspace",href:"/admin/new-company",keywords:"client portal company"}];
export default function AdminCommandPalette(){const pathname=usePathname();const[open,setOpen]=useState(false);const[query,setQuery]=useState("");const[remote,setRemote]=useState<Command[]>([]);useEffect(()=>{function key(e:KeyboardEvent){if((e.metaKey||e.ctrlKey)&&e.key.toLowerCase()==="k"){e.preventDefault();setOpen(v=>!v)}if(e.key==="Escape")setOpen(false)}window.addEventListener("keydown",key);return()=>window.removeEventListener("keydown",key)},[]);useEffect(()=>{if(!open||query.trim().length<2){setRemote([]);return}const timer=setTimeout(()=>fetch(`/api/admin/search?q=${encodeURIComponent(query)}`).then(r=>r.ok?r.json():[]).then(d=>setRemote(Array.isArray(d)?d:[])).catch(()=>setRemote([])),180);return()=>clearTimeout(timer)},[query,open]);const results=useMemo(()=>{const q=query.toLowerCase();const local=base.filter(x=>!q||`${x.label} ${x.detail} ${x.keywords}`.toLowerCase().includes(q));return [...remote,...local].filter((x,i,a)=>a.findIndex(y=>y.href===x.href&&y.label===x.label)===i).slice(0,16)},[query,remote]);if(!pathname?.startsWith("/admin"))return null;return <>{open&&<div className="admin-command-backdrop" onMouseDown={()=>setOpen(false)}><section className="admin-command-palette" onMouseDown={e=>e.stopPropagation()}><div className="admin-command-search"><span>⌕</span><input autoFocus value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search companies, engagements, work orders, tasks, assets..."/><kbd>ESC</kbd></div><div className="admin-command-results">{results.length?results.map((x,i)=><Link key={`${x.href}-${x.label}-${i}`} href={x.href} onClick={()=>{setOpen(false);setQuery("")}}><strong>{x.label}</strong><span>{x.type?`${x.type} · `:""}{x.detail}</span></Link>):<p>No matching workspace content.</p>}</div><footer><span>Universal UPZ search</span><span>⌘K / Ctrl+K</span></footer></section></div>}</>}
